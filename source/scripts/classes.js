const active = '-is-active';
const fixed = '-is-fixed';
const visible = '-is-visible';
const hidden = '-is-hidden';
const open = '-is-open';
const noScroll = '-no-scroll';
const jsHide = 'js-hide';
const menuShowing = '-menu-showing';
const searchShowing = '-search-showing';
const keyup = '-keyup';

export {
    active,
    fixed,
    visible,
    hidden,
    open,
    noScroll,
    jsHide,
    menuShowing,
    searchShowing,
    keyup,
};